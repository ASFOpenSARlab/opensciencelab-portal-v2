---
name: Linting and Formatting
on: # yamllint disable-line rule:truthy
  pull_request: {}
  workflow_dispatch: {}
jobs:
  shell:
    uses: ASFOpenSARlab/osl-utils/.github/workflows/reusable-code-quality-shell.yaml@v1.2.1
    with:
      osl-utils-tag: v1.2.1
  python:
    uses: ASFOpenSARlab/osl-utils/.github/workflows/reusable-code-quality-python.yaml@v1.2.1
    with:
      osl-utils-tag: v1.2.1
  jinja2:
    uses: ASFOpenSARlab/osl-utils/.github/workflows/reusable-code-quality-jinja2.yaml@v1.2.1
    with:
      osl-utils-tag: v1.2.1
  # These yaml lints aren't working
  # yaml:
  #   uses: ASFOpenSARlab/osl-utils/.github/workflows/reusable-code-quality-yaml.yaml@v1.2.1
  #   with:
  #     osl-utils-tag: v1.2.1
  dockerfile:
    uses: ASFOpenSARlab/osl-utils/.github/workflows/reusable-code-quality-dockerfile.yaml@v1.2.1
    with:
      osl-utils-tag: v1.2.1
  markdown:
    uses: ASFOpenSARlab/osl-utils/.github/workflows/reusable-code-quality-markdown.yaml@v1.2.1
    with:
      # The osl-utils docker tag. Should match the above tag in prod.
      # Other potential values: `prod`, `test`, `dev`, <dev-initials>, <commit-hash>
      osl-utils-tag: v1.2.1
